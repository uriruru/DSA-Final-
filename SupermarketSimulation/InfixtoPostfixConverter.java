/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package list;

/**
 *
 * @author gudz
 */
import java.io.*;

public class InfixtoPostfixConverter  {
    public static void main (String [] args){
        StringBuffer infix = new StringBuffer("(6+2)*5-8/4"); //infix buffer
        StringBuffer postfix = new StringBuffer(); //postfix buffer
        StackInheritance stack = new StackInheritance();

        //a
        stack.push('(');

        //b
        infix.append(")");

        //c
        while( )
        {
            if( ) //If the current character in infix is a digit
            {

            }

            if ( ) //If the current character in infix is a left parenthesis
            {

            }

            if( )//If the current character in infix is an operator:
            {

            }

            if ( )// If the current character in infix is a right parenthesis:
            {

            }

        }

        boolean isOperator (char C)
        {

        }

        boolean precendence (char op1, char op2)
        {

        }


}


